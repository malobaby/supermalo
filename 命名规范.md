# Supermalo 命名规范 v1

> **状态**：已定稿（2026-06-04）  
> **适用范围**：`supermalo` 仓库全部可安装资产及 Node CLI 安装器  
> **背景**：自 [ai-max](https://github.com/zhukunpenglinyutong/ai-max) 复制并 rebranding；与 Superpowers 等插件并存时，通过 `malo-` 前缀区分自有资产。

---

## 1. 设计目标

1. **品牌独立**：仓库、npm 包、**终端 CLI** 均为 `supermalo`；Claude **斜杠命名空间** 为 `malo`（`/malo:malo-…`），不与 `aimax` 混用。
2. **可发现性**：commands、agents、rules、skills 统一 `malo-` 前缀，便于在 `~/.claude` 内搜索、与第三方插件区分。
3. **一致性**：文件名、frontmatter `name`、文档引用、斜杠调用四者对齐。
4. **可追溯**：`NOTICE` 等致谢处可保留 `ai-max` / `everything-claude-code` 字样；其余代码与文档清零旧品牌。

---

## 2. 命名分层

| 层级 | 标识 | 示例 |
|------|------|------|
| 仓库 / 品牌 | `supermalo` | GitHub 仓库、README 标题「Supermalo」 |
| npm 包名 | `supermalo` | `package.json` → `"name": "supermalo"` |
| CLI 可执行名 | `supermalo` | `bin.supermalo` → 终端执行 `supermalo install`（与 npm 包名一致，Node 常见做法） |
| Claude 斜杠命名空间 | `malo` | 安装目录 `~/.claude/commands/malo/`（与 CLI 名分离，见 §8.1） |
| 斜杠命令 stem | `malo-<动词>` | 见 §4 |
| Agent 标识 | `malo-<角色>` | `malo-planner` |
| Rules 文件名 | `malo-<主题>` | `malo-security.md` |
| Skills 目录/文件 | `malo-<主题>` | `malo-tdd-workflow/` |
| 本机安装元数据 | `supermalo` | `~/.claude/.supermalo-version` |

**原则**：终端与 npm 用 **supermalo**；Claude 内用 **/malo:malo-…** 与 **malo-** 资产前缀；三者分工见 §8.1。

---

## 3. 通用格式规则

- **大小写**：全小写。
- **分隔符**：kebab-case（`malo-code-review`），禁止 `malo_code_review`、驼峰。
- **前缀**：commands、agents、rules、skills 的文件名与 skill 的 `name:` 均带 `malo-`。
- **禁止残留**（致谢文件除外）：`aimax`、`AI MAX`、`/aimax:`、`commands/aimax`、`.aimax-version`、`getAimaxDir` 等。

---

## 4. 斜杠命令（方案 A：双 malo）

### 4.1 机制

- 安装目标：`COMPONENTS.commands.target = 'commands/malo'`
- 源文件位于仓库 `commands/malo-<stem>.md`
- 安装后：`~/.claude/commands/malo/malo-<stem>.md`
- 用户调用：**`/malo:malo-<stem>`**（命名空间 `malo` + stem `malo-<stem>`）

### 4.2 命令对照表

| 原 aimax 文件 | 原斜杠 | 新文件 | 新斜杠 |
|---------------|--------|--------|--------|
| `auto.md` | `/aimax:auto` | `malo-auto.md` | `/malo:malo-auto` |
| `plan.md` | `/aimax:plan` | `malo-plan.md` | `/malo:malo-plan` |
| `tdd.md` | `/aimax:tdd` | `malo-tdd.md` | `/malo:malo-tdd` |
| `e2e.md` | `/aimax:e2e` | `malo-e2e.md` | `/malo:malo-e2e` |
| `code-review.md` | `/aimax:code-review` | `malo-code-review.md` | `/malo:malo-code-review` |
| `build-fix.md` | `/aimax:build-fix` | `malo-build-fix.md` | `/malo:malo-build-fix` |
| `test-coverage.md` | `/aimax:test-coverage` | `malo-test-coverage.md` | `/malo:malo-test-coverage` |
| `refactor-clean.md` | `/aimax:refactor-clean` | `malo-refactor-clean.md` | `/malo:malo-refactor-clean` |
| `update-docs.md` | `/aimax:update-docs` | `malo-update-docs.md` | `/malo:malo-update-docs` |
| `update-codemaps.md` | `/aimax:update-codemaps` | `malo-update-codemaps.md` | `/malo:malo-update-codemaps` |

### 4.3 `malo-auto` 路由

- 路由表、示例、输出模板中的目标命令使用 **stem 全名**（如 `malo-plan`、`malo-build-fix`），勿用无前缀的 `plan`、`build-fix`。
- 展示给用户时使用 **`/malo:malo-<stem>`**。

---

## 5. Agents

- **路径**：`agents/malo-<角色>.md` → `~/.claude/agents/malo-<角色>.md`
- **frontmatter**：`name: malo-<角色>`（与文件名一致，不含 `.md`）
- **文档引用**：`rules/malo-agents.md`、各 command 正文统一写 **malo-planner** 等，路径示例：

  `~/.claude/agents/malo-planner.md`

### 5.1 Agent 对照表

| 原文件 | 原 `name` | 新文件 | 新 `name` |
|--------|-----------|--------|-----------|
| `planner.md` | `planner` | `malo-planner.md` | `malo-planner` |
| `architect.md` | `architect` | `malo-architect.md` | `malo-architect` |
| `tdd-guide.md` | `tdd-guide` | `malo-tdd-guide.md` | `malo-tdd-guide` |
| `code-reviewer.md` | `code-reviewer` | `malo-code-reviewer.md` | `malo-code-reviewer` |
| `security-reviewer.md` | `security-reviewer` | `malo-security-reviewer.md` | `malo-security-reviewer` |
| `build-error-resolver.md` | `build-error-resolver` | `malo-build-error-resolver.md` | `malo-build-error-resolver` |
| `e2e-runner.md` | `e2e-runner` | `malo-e2e-runner.md` | `malo-e2e-runner` |
| `refactor-cleaner.md` | `refactor-cleaner` | `malo-refactor-cleaner.md` | `malo-refactor-cleaner` |
| `doc-updater.md` | `doc-updater` | `malo-doc-updater.md` | `malo-doc-updater` |

### 5.2 Command 与 Agent 对应关系

| Command | 主要 Agent |
|---------|------------|
| `malo-plan` | `malo-planner` |
| `malo-tdd` | `malo-tdd-guide` |
| `malo-e2e` | `malo-e2e-runner` |
| `malo-code-review` | `malo-code-reviewer` |
| `malo-build-fix` | `malo-build-error-resolver` |
| `malo-refactor-clean` | `malo-refactor-cleaner` |
| `malo-update-docs` | `malo-doc-updater` |

（`malo-test-coverage`、`malo-update-codemaps`、`malo-auto` 按各 command 文档原有说明引用 agent/skill。）

---

## 6. Rules

- **路径**：`rules/malo-<主题>.md` → `~/.claude/rules/malo-<主题>.md`
- 当前无 YAML frontmatter；**以文件名为准**。
- 用户项目 `CLAUDE.md` 中引用规则时，应写 `malo-security` 等新名。

### 6.1 Rules 对照表

| 原文件 | 新文件 |
|--------|--------|
| `agents.md` | `malo-agents.md` |
| `coding-style.md` | `malo-coding-style.md` |
| `git-workflow.md` | `malo-git-workflow.md` |
| `hooks.md` | `malo-hooks.md` |
| `patterns.md` | `malo-patterns.md` |
| `performance.md` | `malo-performance.md` |
| `security.md` | `malo-security.md` |
| `testing.md` | `malo-testing.md` |

---

## 7. Skills

- **目录型**：`skills/malo-<主题>/SKILL.md`，frontmatter `name: malo-<主题>`
- **单文件型**：`skills/malo-<主题>.md`，frontmatter `name: malo-<主题>`
- 安装后路径与仓库相对结构一致，位于 `~/.claude/skills/`
- 与 Superpowers 等插件并存时，新增自有 skill 一律 `malo-*`（如未来 `malo-brainstorm`），避免与 `brainstorming`、`writing-plans` 等短名冲突。

### 7.1 Skills 对照表

| 原路径 | 新路径 | 新 `name` |
|--------|--------|-----------|
| `tdd-workflow/` | `malo-tdd-workflow/` | `malo-tdd-workflow` |
| `security-review/` | `malo-security-review/` | `malo-security-review` |
| `coding-standards.md` | `malo-coding-standards.md` | `malo-coding-standards` |
| `backend-patterns.md` | `malo-backend-patterns.md` | `malo-backend-patterns` |
| `frontend-patterns.md` | `malo-frontend-patterns.md` | `malo-frontend-patterns` |
| `clickhouse-io.md` | `malo-clickhouse-io.md` | `malo-clickhouse-io` |
| `project-guidelines-example.md` | `malo-project-guidelines-example.md` | `malo-project-guidelines-example`（当前无 `name:`，需新建 frontmatter） |

### 7.2 交叉引用

凡 command 或 skill 内引用 skill 路径，须更新为例如：

`~/.claude/skills/malo-tdd-workflow/`

---

## 8. Node 安装器与 CLI

### 8.1 CLI 与斜杠命名空间（选项 A：主命令 `supermalo`）

| 场景 | 标识 | 说明 |
|------|------|------|
| 终端 / npm | `supermalo` | `supermalo install`、`supermalo update -y`，与包名一致 |
| Claude 斜杠 | `malo` | `/malo:malo-plan`，命名空间不必与 CLI 同名 |
| 资产文件名 | `malo-*` | agents、rules、skills、command stem |

不提供 `malo` 作为 CLI 别名，避免与斜杠命名空间混淆、并减少全局命令冲突。

### 8.2 安装器配置

| 项目 | 规范 |
|------|------|
| `package.json` `name` | `supermalo` |
| `bin` | `{ "supermalo": "./bin/cli.js" }` |
| Commander `program.name()` | `supermalo` |
| 版本文件 | `~/.claude/.supermalo-version` |
| `COMPONENTS.commands.target` | `commands/malo` |
| 卸载时删除的 commands 目录 | `~/.claude/commands/malo` |
| 工具函数命名 | `getAimaxDir` → 改为 `getSupermaloDir` |
| 用户可见文案 | 产品名「Supermalo」；终端示例 `supermalo install`；斜杠示例 `/malo:malo-plan` |

**暂不自动安装**（与 aimax 一致，README 说明手动步骤）：`hooks/`、`mcp-configs/`。若日后纳入 installer，路径命名仍建议带 `malo-` 或置于 `supermalo` 子目录并文档化。

---

## 9. 致谢与例外

- **`NOTICE`**（及 LICENSE 必要致谢）：可保留 `ai-max`、`everything-claude-code` 等原名。
- **正文、源码、测试、README、examples**：不得出现可操作的旧命令 `/aimax:*` 或旧安装路径（除非明确标注为「上游历史」说明）。

---

## 10. 与旧版 aimax 共存 / 迁移

若本机曾安装 aimax，建议在安装 supermalo 前或后：

1. 卸载 aimax 或手动删除 `~/.claude/commands/aimax/`、`.aimax-version`。
2. 检查 `~/.claude/rules/`、`agents/`、`skills/` 是否残留**无前缀**的 aimax 同名文件，避免与 `malo-*` 重复生效。
3. 更新个人 `CLAUDE.md` 中的规则与 skill 引用为新名称。

---

## 11. Rebrand 实施检查清单

- [ ] `package.json`、`bin/cli.js`、`src/utils.js`、`src/installer.js`、`src/index.js`、`src/prompts.js`
- [ ] `LICENSE`（追加 supermalo 版权行，保留上游致谢）
- [ ] `tests/*`（版本路径、commands 目标目录、`getAimaxDir` → `getSupermaloDir`）
- [ ] 重命名 `commands/`（10）、`agents/`（9）、`rules/`（8）、`skills/`（7 项）
- [ ] 更新所有 skill `name:`、agent `name:`
- [ ] `commands/malo-auto.md`（重命名后）路由表与全文 `/malo:malo-*`
- [ ] `rules/malo-agents.md`（重命名后）与各 command 内 agent/skill 路径
- [ ] `README.md`、`examples/`、`NOTICE`（新建）
- [ ] `COMPONENTS.commands` 的 `name` 与 `description`（`src/utils.js`）
- [ ] `bin/cli.js` 与 `src/index.js` 中 `docs` 命令的 URL
- [ ] 全文搜索：`aimax`、`/aimax:`、`commands/aimax`（排除 NOTICE/致谢）
- [ ] `npm test` → `npm link` → `supermalo install -y` 验证

---

## 12. 快速检索

在仓库或 `~/.claude` 中搜索前缀 **`malo-`** 应覆盖 Supermalo 全部自有 commands（安装后位于 `commands/malo/`）、agents、rules、skills。

- 终端：`supermalo install`（及 `update`、`uninstall` 等子命令）
- 斜杠：**`/malo:malo-<命令>`**
